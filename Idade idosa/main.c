#include <stdio.h>
#include <stdlib.h>

void idosa (void)
{
    int idade, anoatual, anonascimento;

    printf("Ano atual: ");
    scanf("%d", &anoatual);
    printf("Ano de nascimento: ");
    scanf("%d", &anonascimento);
    printf("Idade do individuo: %d\n", idade = anoatual - anonascimento);
    if (idade >= 51)
    {
        printf("O individuo e idoso\n");
    }
    else if (idade );
}

int main ()
{
    idosa ();
    return 0;
}
